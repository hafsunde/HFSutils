% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icc_openmx.R
\name{mx.ICC}
\alias{mx.ICC}
\title{Estimate ICC using an OpenMx model with exchangeable covariance structure}
\usage{
mx.ICC(
  dt,
  group_id,
  phenotype,
  group.max = 200,
  is_dichotmous = FALSE,
  factor.levels = c(0, 1)
)
}
\arguments{
\item{dt}{Input data frame/data.table.}

\item{group_id}{Column name (character scalar) identifying clusters.}

\item{phenotype}{Column name (character scalar) for the outcome.}

\item{group.max}{Maximum number of individuals per group retained for model
fitting.}

\item{is_dichotmous}{Logical; whether to fit a dichotomous (liability
threshold) model.}

\item{factor.levels}{Ordered outcome levels for dichotomous models.}
}
\value{
A fitted \code{MxModel} object with an \code{ICC} algebra.
}
\description{
Fits an OpenMx model after reshaping long data to wide format (one row per
group), using an exchangeable covariance structure. In this structure, all
diagonal covariance-matrix elements share one variance parameter (\code{V}) and
all off-diagonal elements share one covariance parameter (\code{r}). This
constrains all repeated measures to have equal variance and every pair to
have equal covariance. The ICC is then computed as \code{r / V}. For dichotomous
outcomes, the same exchangeable covariance structure is estimated under a
liability-threshold model.
}
