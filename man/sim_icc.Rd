% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_icc.R
\name{sim_icc}
\alias{sim_icc}
\title{Simulate clustered outcomes with a target ICC}
\usage{
sim_icc(num_groups, num_individuals, ICC, prevalence = NULL)
}
\arguments{
\item{num_groups}{Number of clusters/groups to simulate.}

\item{num_individuals}{Number of individuals per cluster.}

\item{ICC}{Target intraclass correlation coefficient on the latent scale.
Must be between 0 and 1.}

\item{prevalence}{Optional target prevalence for a binary outcome.
If \code{NULL} (default), returns a continuous latent outcome.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{Group}{Cluster identifier as a factor.}
\item{Individual}{Row-level individual identifier.}
\item{Y}{Simulated outcome (continuous or binary).}
}
}
\description{
Simulate clustered data under a random-intercept model where the total latent
variance is fixed to 1 and split into between-cluster variance (\code{ICC}) and
within-cluster variance (\code{1 - ICC}). Optionally dichotomize the latent outcome
to a binary variable using a threshold chosen to match a target prevalence.
}
\examples{
d_cont <- sim_icc(num_groups = 100, num_individuals = 3, ICC = 0.30)
d_bin <- sim_icc(num_groups = 100, num_individuals = 3, ICC = 0.30, prevalence = 0.10)

}
